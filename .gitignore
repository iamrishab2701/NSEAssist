*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
*.jks
*.keystore

# App build outputs — APK, AAB, baseline profiles, metadata (never commit these)
app/build/
app/release/

# Gradle wrapper jar (generated by Android Studio)
gradle/wrapper/gradle-wrapper.jar

# Java heap dumps (Android Studio crash/profiler — never commit these)
*.hprof
*.prof

# Firebase — contains project IDs, keep out of version control
app/google-services.json

# Cloudflare Worker secrets
cloudflare-worker/.dev.vars

# Logs
*.log
